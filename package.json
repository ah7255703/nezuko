{
  "name": "with-docker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "env-cmd turbo run build",
    "clean": "turbo run clean",
    "dev": "env-cmd turbo run dev",
    "start": "env-cmd turbo run start",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "compose": "docker-compose up -d",
    "db:generate": "env-cmd -- pnpm -F napi generate",
    "db:migrate": "env-cmd -- pnpm -F napi migrate"
  },
  "dependencies": {
    "env-cmd": "^10.1.0"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "prettier": "^3.2.5",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=7.0.0"
  },
  "packageManager": "pnpm@9.0.6"
}